# Production Decision Sandbox GitHub Pages 主页方案

> 版本：V0.2  
> 日期：2026-08-25  
> 承载项目：`production-decision-sandbox`  
> 方案性质：公开主页、静态教学沙盒与生态入口的实施基线

## 1. 方案结论

GitHub Pages 主页应直接建设在 `production-decision-sandbox` 项目中，并形成两个彼此衔接、职责清晰的部分：

1. **公开主页**：负责品牌识别、价值表达、工程证据、用户分流和合作入口；
2. **公开沙盒**：负责数据诊断、流程编排、规则案例、AI Agent 任务组织和 DecisioWorks 教学的静态交互体验。

现有 `index.html` 已经具备沙盘原型，应在实施时迁入 `/sandbox/`，保留其交互价值；仓库根目录的新 `index.html` 用于建设公开主页。这样可以避免首页同时承担宣传、教学和工具操作，导致内容拥挤、入口混乱。

主页的核心任务不是在线成交，也不复制 DecisioWorks Web 驾驶舱，而是让访问者完成一条清晰路径：

`看懂 DecisioWorks -> 进入真实样例 -> 理解能力链 -> 选择研究版/商业版/生态合作入口`

## 2. 产品与品牌关系

### 2.1 主品牌表达

- 页面主标题：`DecisioWorks 制造业生产决策工具包`
- 页面副标题：`从业务数据到受控求解、结果解释与反馈演化的生产决策能力链`
- 沙盒名称：`Production Decision Sandbox / 生产决策沙盘`
- 沙盒定位：`DecisioWorks 的可视化学习、场景验证与生态协作入口`

首页第一视觉信号应是 **DecisioWorks**。Production Decision Sandbox 作为体验载体出现，避免访问者误以为这是一个与 DecisioWorks 无关的新产品。

### 2.2 能力层次

| 层次 | 对外职责 | 主页呈现方式 |
|---|---|---|
| DataSets | 行业场景、业务对象与标准数据接口模板 | 真实数据表、关系图和场景案例 |
| DecisioCore | 公开的生产决策能力组织与参考实现 | 架构说明、源码入口、开发文档 |
| GOCK | 模型、优化与受控求解能力 | 能力角色说明，不展示受保护实现 |
| Web | 数据、配置、运行、结果和分析的可视化操作 | 真实界面截图与操作入口 |
| Production Decision Sandbox | 教学、静态体验、场景整理与生态协作 | GitHub Pages 公开站点 |

### 2.3 版本与入口关系

- **研究版**：学习、查看样例、理解能力边界和本地验证；
- **商业版**：面向实际应用、持续运行、商业授权与技术支持；
- **DecisioCore**：开放的参考实现，供开发者理解、调用和扩展生产决策能力；
- **Sandbox**：帮助不同角色在下载或接入前先看懂数据、流程、规则和结果。

主页不直接绑定专利表述，先以 DecisioWorks 的产品、工程能力和公开样例建立认知。

## 3. 目标访问者与首要路径

首页按访问目的分流，而不是按内部模块分流。

| 访问者 | 首要问题 | 推荐路径 | 最终动作 |
|---|---|---|---|
| 制造企业 | 数据和场景能否进入生产决策链 | 场景案例 -> 数据诊断 -> 商业版 | 整理场景或沟通试点 |
| 咨询、集成和软件伙伴 | 如何复用并嵌入现有能力 | 能力架构 -> DecisioCore -> 生态合作 | 查看集成方式 |
| 开发者与 AI Agent 使用者 | 如何调用、编排和扩展能力 | 快速开始 -> 流程编排 -> 开发文档 | 查看源码或运行样例 |
| 园区与产业机构 | 项目能形成什么产业协作 | 项目概览 -> 场景与生态 -> 联系 | 对接场景或合作资源 |

首屏提供三个主动作：

1. `进入生产决策沙盘`；
2. `查看 DecisioCore`；
3. `了解商业版`。

其中第一个是主按钮，后两个为次级动作。

## 4. 站点信息架构

### 4.1 推荐路由

| 路径 | 页面 | 核心内容 |
|---|---|---|
| `/` | 公开主页 | 定位、证据、能力链、用户分流和合作入口 |
| `/sandbox/` | 生产决策沙盘 | 现有静态原型、数据诊断、参数调整和结果演示 |
| `/examples/` | 场景与示例 | 按场景、业务属性和就绪状态组织案例 |
| `/learn/` | 学习路径 | 数据接入、目标规则、求解分析、AI Agent 教学 |
| `/docs/` | 文档入口 | 快速开始、标准数据接口、DecisioCore、API 与排障 |
| `/ecosystem/` | 生态合作 | 制造企业、咨询伙伴、集成伙伴和园区合作路径 |
| `/editions/` | 版本与下载 | 研究版、商业版、版本说明、校验和项目地址 |
| `/contact/` | 联系 | 公众号、邮箱、项目地址和合作说明 |

首期不必一次完成所有独立页面。可以先用首页锚点和 `/sandbox/` 双页面上线，但目录和链接命名应从第一天保持稳定。

### 4.2 顶部导航

- 能力链
- 场景示例
- 生产决策沙盘
- 学习与文档
- 版本与下载
- 生态合作

导航右侧保留：

- GitHub 图标按钮；
- Gitee 图标按钮；
- `进入沙盘` 主按钮。

移动端收纳为菜单，主按钮始终可见。

## 5. 首页内容顺序

### 5.1 首屏：先回答“是什么”

首屏采用 DecisioWorks 真实 Web 界面或沙盒能力链作为全宽背景，叠加纯色遮罩保证文字清晰，不使用抽象渐变、装饰光球或营销卡片。

- H1：`DecisioWorks 制造业生产决策工具包`
- 支撑文字：`让数据、目标、规则、求解、结果与反馈进入同一条可运行、可解释、可持续迭代的能力链。`
- 主按钮：`进入生产决策沙盘`
- 次按钮：`查看 DecisioCore 源码`
- 文本链接：`了解商业版`

首屏高度控制在视口的 72% 至 82%，底部必须露出下一节标题，避免形成封闭的宣传画面。

### 5.2 工程状态条：给出可信证据

使用一条紧凑状态带，展示可核验事实：

- 研究版已公开发布；
- 商业版已就绪；
- DecisioCore 参考实现已开放；
- 已有真实行业数据模板和可运行示例；
- Windows / Linux 多环境支持。

不使用未经当前版本核对的固定数量，不使用“领先”“唯一”等难以验证的形容词。

### 5.3 四段能力链：回答“解决什么”

用四个横向阶段替代功能卡片墙：

1. **业务可表达**：订单、工艺、产能、物料、日历和班制进入统一数据结构；
2. **决策可组织**：目标、经验、偏好、规则和计划变化进入可检查的决策链；
3. **问题可求解**：模型与求解器在明确配置下形成计划与排程结果；
4. **结果可演化**：分析偏差、解释结果，并把反馈带入下一轮决策。

每个阶段只保留一句关键说明和一个“查看证据”入口。

### 5.4 项目层次：解释各部分怎样协作

使用稳定、清晰的层次图：

`行业数据模板 -> DecisioCore 决策能力链 -> GOCK 模型与优化 -> Web / Sandbox 展示与教学`

图中必须明确：GOCK 负责优化并形成好计划，DecisioCore 负责围绕计划组织好决策，Web 与 Sandbox 负责让过程可见、可操作、可学习。

### 5.5 沙盒体验：把理解变成操作

本节不放静态概述，而是展示真实沙盒界面并提供三个直接入口：

- `检查输入数据`；
- `查看能力编排`；
- `调整参数并比较结果`。

GitHub Pages 中只运行静态公开样例。按钮和状态必须明确区分“静态教学演示”与“本地真实求解”。

### 5.6 场景案例：从输入走到结果

首批案例只选择内容完整、已进入求解链路且适合公开的场景。每个案例使用相同结构：

1. 场景与业务问题；
2. 输入数据表；
3. 关键约束和目标；
4. 处理流程；
5. 输出结果与解释；
6. 就绪状态与下一步动作。

首页只精选 3 至 6 个案例，完整案例进入 `/examples/`。

### 5.7 开放与生态：说明如何参与

本节突出三项竞争差异：

- **数据开放接入**：标准数据接口帮助不同信息化基础的企业进入同一能力链；
- **能力开放组合**：DecisioCore 提供公开参考实现和稳定调用方式；
- **生态开放扩展**：算法、模型、咨询方法、行业模板和外围应用可以保持边界并协同演进。

对应给出四类入口：场景共建、模板共建、系统集成、教学与试点。

### 5.8 版本分流：研究版与商业版

采用紧凑比较表，不使用复杂套餐营销样式。

| 需求 | 推荐入口 |
|---|---|
| 学习、查看结构和运行公开样例 | 研究版 |
| 评估源码、接口和扩展方式 | DecisioCore / 文档 |
| 面向实际业务运行与持续支持 | 商业版 |
| 组织培训、诊断和生态协作 | Production Decision Sandbox |

### 5.9 最终行动区

页面底部只保留一条明确表达：

> 欢迎制造企业、园区机构、产业伙伴及生态合作方，围绕 AI+生产决策、智能制造与工业软件工具包化开展交流与合作，有意者可留言或私信。

提供公众号二维码、邮箱、GitHub、Gitee 和商业版沟通入口。

## 6. 专题页设计

### 6.1 `/sandbox/` 生产决策沙盘

现有原型迁移到该路径，重点改造成五个稳定工作区：

1. 数据与场景选择；
2. 数据就绪度和关系诊断；
3. 目标、偏好与规则配置；
4. 能力链和流程编排；
5. 结果分析、方案对比与下一轮建议。

页面应使用工具型布局，不重复首页的品牌介绍。

### 6.2 `/examples/` 场景与示例

支持按以下维度筛选：

- 生产场景；
- 计划层级；
- 数据属性；
- 约束类型；
- 可运行能力；
- ContractReady / ModelReady / SolverReady / 端到端验证状态。

状态必须来自公开数据记录，不能由页面根据文件存在情况自行推断。

### 6.3 `/learn/` 学习路径

按角色组织：

- 制造企业与计划人员；
- 数据与实施人员；
- 咨询与集成伙伴；
- 开发者；
- AI Agent 使用者。

每条路径明确前置材料、练习步骤、检查点和最终产出。

### 6.4 `/docs/` 文档入口

文档按任务组织，而不是按仓库目录罗列：

- 快速运行；
- 数据接入；
- 标准动作与能力编排；
- 目标、偏好和规则；
- 求解配置；
- 结果分析；
- 计划联动；
- 扩展开发；
- 故障排查。

### 6.5 `/ecosystem/` 生态合作

分别说明制造企业、咨询伙伴、MES/ERP/WMS 厂商、开发者和园区机构能够贡献什么、获得什么、从哪里开始。

## 7. 视觉与交互规范

### 7.1 视觉基线

- 默认浅色，整体保持安静、专业、工程化；
- 背景使用白色和冷灰，正文使用深墨色；
- 蓝色用于主操作，绿色用于就绪与反馈，橙色用于规则与风险，紫色用于目标与治理；
- 不使用一整页同色系深蓝，不使用渐变背景和装饰光斑；
- 卡片圆角不超过 8px；
- 页面区段采用全宽带状结构，避免卡片套卡片；
- 标题不随视口宽度缩放，移动端通过断点换行。

### 7.2 品牌资产

- DecisioWorks 图标用于导航、favicon 和页面署名；
- 首屏主视觉优先使用真实 Web / Sandbox 画面；
- 项目层次图和能力链图使用统一线宽、节点尺寸与箭头规范；
- 所有流程箭头从节点边缘中心连接，文本在容器内水平和垂直居中；
- 不在图片中出现编号、编辑说明、内部路径和过程性文字。

### 7.3 交互原则

- 首页导航和按钮只执行清晰跳转；
- 数据标签和术语悬停后显示业务解释；
- 折叠区域使用明显的展开/收起图标和状态文字；
- 固定格式控件预留稳定尺寸，避免内容变化引起布局跳动；
- 移动端所有表格使用横向容器或摘要视图，不让页面整体横向溢出。

## 8. 技术实施方案

### 8.1 首期技术选择

首期继续使用原生 HTML、CSS 和 JavaScript，不引入 React、Astro 或后端服务。原因是：

- 当前原型已经采用原生静态技术；
- GitHub Pages 可直接托管；
- 无需安装依赖和维护构建链；
- 更适合当前以内容、样例和静态交互为主的阶段。

当流程编排器出现复杂拖拽、节点状态同步和大量组件复用需求时，再单独评估 React + TypeScript + Vite。框架迁移不是主页上线的前置条件。

### 8.2 推荐目录

```text
production-decision-sandbox/
  index.html
  404.html
  robots.txt
  sitemap.xml
  sandbox/
    index.html
    app.js
    styles.css
  examples/
    index.html
  learn/
    index.html
  docs/
    index.html
  ecosystem/
    index.html
  editions/
    index.html
  contact/
    index.html
  assets/
    brand/
    screenshots/
    diagrams/
    icons/
  public-data/
    site.json
    capability-map.json
    examples.json
    lessons.json
    control-cases.json
    downloads.json
  shared/
    site.css
    site.js
  scripts/
    validate-public-content.mjs
    check-links.mjs
  .github/
    workflows/
      pages.yml
```

所有内部链接使用相对路径或统一的站点基础路径，兼容 `https://<account>.github.io/<repository>/` 项目站点。

### 8.3 内容数据模型

公开内容使用静态 JSON，页面只负责渲染：

- `site.json`：导航、版本、项目地址和联系入口；
- `capability-map.json`：能力层次和角色；
- `examples.json`：场景、状态、输入、配置、结果和证据；
- `lessons.json`：学习路径和检查点；
- `control-cases.json`：经验、偏好和规则案例；
- `downloads.json`：版本、平台、文件、体积和 SHA256。

主页事实尽量由这些文件统一提供，减少同一版本号、项目地址或状态在多处手工维护。

### 8.4 GitHub Pages 发布

使用 GitHub Actions 自定义工作流发布静态站点：

- Pages 发布源选择 `GitHub Actions`；
- `main` 分支推送触发部署；
- 工作流至少授予 `contents: read`、`pages: write` 和 `id-token: write`；
- 使用 `actions/configure-pages`、`actions/upload-pages-artifact` 和 `actions/deploy-pages`；
- 部署环境使用 `github-pages`；
- 发布前执行 HTML、JSON、链接和敏感信息检查；
- 发布产物不超过 GitHub Pages 的站点限制，不在仓库中提交发行压缩包。

GitHub Pages 仅用于公开说明、静态体验和项目分流，不承载支付、商业交易、客户数据上传或在线 SaaS 求解。GitHub 官方也明确说明 Pages 是静态站点托管服务，不适合作为在线商业交易或 SaaS 主体。

### 8.5 域名策略

首期使用 GitHub Pages 默认地址验证内容和转化路径。完成公司主体、品牌域名和备案安排后，再接入自定义域名。

自定义域名启用时：

- 同时配置根域与 `www`；
- 开启 `Enforce HTTPS`；
- 仓库保留 `CNAME`；
- 禁止使用通配 DNS；
- 检查页面不存在 `http://` 混合内容。

## 9. 公开内容与安全边界

### 9.1 可以发布

- 产品和能力说明；
- DecisioCore 公开结构与源码入口；
- 脱敏数据样例、静态关系图和公开教程；
- 研究版、商业版说明和下载指引；
- 版本、文件体积与 SHA256；
- 公开提示词模板；
- 生态合作与联系信息。

### 9.2 不进入 Pages

- 客户原始数据和客户项目记录；
- 私钥、授权文件、签发能力和授权请求；
- 真实运行日志、临时目录和本机绝对路径；
- GOCK 受保护实现和商业秘密；
- 商业版发行压缩包本体；
- 未审核的内部计划、测试草稿和过程性说明。

所有下载跳转到 GitHub/Gitee Release 或正式发布入口，不把大文件放入 Pages 站点。

## 10. 搜索、传播与度量

- 每个页面设置独立 `title`、description、canonical 和 Open Graph 图片；
- 提供 `sitemap.xml`、`robots.txt`、favicon 和 404 页面；
- 图片提供准确的 `alt`；
- 中文为主，首页和项目简介提供英文摘要；
- 公众号文章、知乎文章、GitHub 和 Gitee 使用同一主页作为稳定入口；
- 首期不收集访客个人数据；确需统计时再引入隐私友好方案，并增加隐私说明。

重点观察四类信号：

1. 首页进入沙盒的比例；
2. 场景案例进入研究版或商业版的比例；
3. DecisioCore 与文档入口点击；
4. 生态合作与联系入口点击。

## 11. 实施阶段

### P0：主页与沙盒分离

1. 备份现有静态原型；
2. 将现有原型迁入 `/sandbox/`；
3. 新建根目录公开主页；
4. 建立共享导航、页脚、品牌资产和公开 JSON；
5. 完成 GitHub Pages 工作流、404、站点地图和基础检查。

**完成标志**：访问者能够看懂项目、进入沙盒、找到 DecisioCore、研究版、商业版和联系入口。

### P1：真实案例与教学路径

1. 接入 3 至 6 个完整公开案例；
2. 增加数据诊断、字段释义和关系图；
3. 增加目标、偏好、规则案例；
4. 增加分角色学习路径；
5. 增加 AI Agent 结构化任务模板。

**完成标志**：访问者不下载软件也能理解一条完整的输入、配置、处理、输出和反馈链。

### P2：生态与传播增强

1. 建立伙伴集成和 OPC Partner Kit 入口；
2. 建立版本与文档轻量搜索；
3. 增加英文摘要页；
4. 接入自定义域名与 HTTPS；
5. 根据真实访问路径调整首页顺序。

**完成标志**：主页能够稳定承接公众号、知乎、GitHub、Gitee 和线下沟通带来的访问，并把不同对象分流到正确入口。

## 12. 验收标准

### 12.1 内容

1. 首次访问者在 10 秒内理解 DecisioWorks 是什么、Sandbox 做什么；
2. 所有能力和状态表达可由公开项目或发行记录核验；
3. 不出现内部口吻、绝对路径、授权材料和过程性说明；
4. 研究版、商业版、DecisioCore 和 Sandbox 的关系清楚。

### 12.2 交互与视觉

1. 桌面 1440px、平板 768px、手机 390px 均无横向溢出；
2. 文字不遮挡、不超出容器；
3. 流程图箭头居中并连接节点边缘；
4. 所有按钮、折叠状态、导航和外链均可识别；
5. 首页首屏露出下一节内容；
6. 图片加载失败时仍能通过替代文本理解内容。

### 12.3 工程

1. GitHub Actions 可重复部署；
2. 站点支持项目子路径；
3. HTML、JSON、站内链接和外链检查通过；
4. 404、sitemap、robots、Open Graph 和 favicon 有效；
5. 发布前敏感信息扫描通过；
6. 桌面和移动端截图验证通过。

## 13. 当前实施决策

1. `production-decision-sandbox` 是 GitHub Pages 的唯一承载项目；
2. 根目录建设公开主页，现有原型迁入 `/sandbox/`；
3. 首期继续使用原生 HTML/CSS/JavaScript；
4. GitHub Pages 只承担公开门户和静态教学，不承载真实求解与客户数据；
5. 首页以 DecisioWorks 为第一品牌信号，以 Sandbox 为体验入口；
6. 内容主线固定为：业务表达、决策组织、受控求解、结果解释、反馈演化；
7. 下一步先完成 P0，再扩充案例、教学和生态页面。

## 14. 官方技术依据

- [GitHub Pages：使用自定义 GitHub Actions 工作流](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)
- [GitHub Pages：配置发布源](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)
- [GitHub Pages：HTTPS](https://docs.github.com/en/pages/getting-started-with-github-pages/securing-your-github-pages-site-with-https)
- [GitHub Pages：自定义域名](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/managing-a-custom-domain-for-your-github-pages-site)
- [GitHub Pages：使用限制](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits)
